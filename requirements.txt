# ICMP差错报文分析程序依赖
# 核心功能不需要额外依赖，使用Python标准库即可

# 可选依赖 - 用于增强功能
scapy>=2.5.0          # 用于pcap文件读取和高级抓包功能

# 注意:
# 1. 基本功能仅使用Python标准库，无需安装额外依赖
# 2. 如需处理pcap文件或使用高级抓包功能，请安装scapy:
#    pip install scapy
# 3. 实时抓包需要管理员权限
